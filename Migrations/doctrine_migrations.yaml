# Doctrine uses a separate migration history table for this plugin so its
# migration history is independent from Kimai core.
table_storage:
    table_name: 'bundle_migration_kimai_expenses_community'

migrations_paths:
    'KimaiPlugin\KimaiExpensesCommunityBundle\Migrations': 'var/plugins/KimaiExpensesCommunityBundle/Migrations'
