# Routes using PHP attributes are discovered automatically by Kimai.
# This file exists so the plugin follows Kimai's standard plugin layout.
mileage_expense:
    resource: '../../Controller/'
    type: attribute
